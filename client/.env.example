# Copy to client/.env (or set as a repo variable in GitHub Actions) to point
# the deployed frontend at your hosted backend. Example:
#   VITE_API_URL=https://campos-api.onrender.com/api
#
# Leave unset for local dev — Vite proxies /api to http://localhost:3001.
