// Shared weekday list — used by the grid component, the admin timetable form,
// and the server-side ordering. One source of truth.
export const DAYS = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
